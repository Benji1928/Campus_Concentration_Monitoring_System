# Virtual environment
face-env/
venv/

# Raw working data — large/untracked source images and extracted CSV features
# (dataset/ is intentionally NOT listed here so it can be committed to git)
data/

# Trained model binary files — too large for git
*.pkl

# Python cache
__pycache__/
*.pyc
*.pyo

models